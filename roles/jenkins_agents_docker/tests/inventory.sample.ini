# Example inventory for role smoke tests.
# 192.0.2.10 is a documentation-only address; replace with a real host in your lab.

[cicd_test]
jenkins-controller-ci ansible_host=192.0.2.10 ansible_user=ansible

jenkins_admin_password: "Temporary!"
jenkins_github_token: "Temporary!"
